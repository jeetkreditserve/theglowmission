services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-theglowmission}
      POSTGRES_USER: ${POSTGRES_USER:-theglowmission}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-theglowmission}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-theglowmission} -d ${POSTGRES_DB:-theglowmission}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: infra/docker/backend.Dockerfile
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "8000"
    restart: unless-stopped
    command: ["/app/scripts/backend-entrypoint.sh"]

  scheduler:
    build:
      context: .
      dockerfile: infra/docker/backend.Dockerfile
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    command:
      [
        "/bin/sh",
        "-c",
        "/app/scripts/wait-for-db.sh postgres:5432 true && python manage.py migrate --noinput && while true; do python manage.py send_appointment_reminders; sleep \"$${APPOINTMENT_SCHEDULER_POLL_SECONDS:-300}\"; done",
      ]

  frontend:
    build:
      context: .
      dockerfile: infra/docker/frontend.Dockerfile
    env_file: .env
    depends_on:
      - backend
    restart: unless-stopped
    expose:
      - "3000"

  nginx:
    build:
      context: .
      dockerfile: infra/docker/nginx.Dockerfile
    depends_on:
      - backend
      - frontend
    restart: unless-stopped
    ports:
      - "8080:1723"
      - "1723:1723"

volumes:
  postgres_data:
